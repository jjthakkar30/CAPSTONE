@model IEnumerable<Hotel>

@* @model TheParkView_CapStone.ViewModels.HotelLocationsRoomTypesViewModel  *@

@{
  ViewData["Title"] = "Home Page";
}

@Html.Partial("_HotelImagesCarousel")

<br>
<br>


<h2 style="margin: 20px 0 20px 20px">Our Locations</h2>
<div class="container-fluid padding">
  <div class="row">
    <div class="row padding">
      @foreach (var item in Model)
      {
        <div class=" col-sm-3 ">
          @Html.Partial("_HotelCard",item)
          <br>
        </div>
      }
    </div>
  </div>
</div>

